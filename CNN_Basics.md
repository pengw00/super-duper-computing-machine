# CNN 卷积神经网络基础

> 基于 train_mnist.py 的知识点总结

---

## 什么是 MNIST CNN

- **MNIST**: 一个经典数据集，包含 7 万张 28×28 像素的手写数字图片（0-9），是深度学习的 "Hello World"
- **CNN**: 卷积神经网络，专门处理图像的神经网络架构
- **MNIST CNN**: 用卷积神经网络学习识别手写数字 — 深度学习最经典的入门项目

**它在干什么**：看 6 万张带答案的手写数字图 → 学会"什么笔画=什么数字" → 对新图片预测是哪个数字（95%+ 准确率）

---

## 涉及的知识点体系（深度学习基础全景）

### 第一层：数学基础
- **线性代数** — 矩阵乘法（forward/backward 的本质）
- **微积分** — 求导、链式法则（反向传播的数学原理）
- **概率** — softmax、交叉熵（损失函数为什么这么设计）

### 第二层：神经网络核心概念
- **前向传播** — 输入 × 权重 + 偏置 → 输出
- **激活函数** — ReLU（为什么需要非线性）
- **损失函数** — 衡量预测和真实答案的差距
- **反向传播** — 链式法则算梯度，知道每个参数该怎么调
- **梯度下降** — 沿梯度方向更新参数
- **学习率** — 每步走多大（太大跳过最优，太小收敛慢）

### 第三层：CNN 特有
- **卷积层** — 局部感受野，参数共享（比全连接省参数）
- **池化层** — 降维，平移不变性
- **特征图** — 每层学到越来越抽象的特征（边缘→纹理→部件→物体）

### 第四层：工程实践
- **Mini-batch SGD** — 为什么不用全部数据也不用单张
- **数据标准化** — 为什么 Normalize 能帮助训练
- **Train/Test 分离** — 防止过拟合
- **GPU 加速** — 并行矩阵运算

---

## 在深度学习知识地图中的位置

```
基础 CNN (MNIST)             ← 当前位置 ✅
     ↓
更深的 CNN (ResNet, ImageNet)
     ↓
RNN / LSTM (序列数据)
     ↓
Transformer / Attention      ← LLM 的基础
     ↓
GPT / LLM 架构              ← Mini Serving Engine 要服务的模型
     ↓
推理优化 (KV Cache, Batching) ← Mini Serving Engine 项目核心
```

---

## 卷积神经网络 (CNN) 是什么

专门处理图像的神经网络。核心思想：用一个小窗口（kernel）在图片上滑动，检测局部模式。

---

## Conv2d — 卷积层（提取空间特征）

想象一个 5×5 的小放大镜在 28×28 的图片上从左到右、从上到下滑动：

```
输入图片 (28×28)          kernel (5×5)         输出特征图 (24×24)
┌──────────────┐         ┌───┐
│ ░░░░░        │         │███│  ← 这个小窗口学会了
│ ░███░        │    *    │███│    识别某种模式
│ ░░░░░        │         │███│    (比如竖线、边缘)
│              │         └───┘
└──────────────┘
```

每滑到一个位置，kernel 和那块区域做点乘求和，得到一个数字。10 个不同的 kernel = 10 张特征图，每张检测不同的模式（横线、竖线、曲线等）。

**关键**：kernel 的参数是训练学出来的，网络自己决定"关注什么特征"。

---

## MaxPool2d — 池化层（降维压缩）

把特征图缩小一半，每 2×2 区域只保留最大值：

```
特征图 (24×24)  →  MaxPool(2)  →  缩小后 (12×12)

┌────┐
│1 3 │ → 取最大值 → 4
│2 4 │
└────┘
```

作用：
1. 减少计算量（图变小了）
2. 提供平移不变性（数字写歪一点也能认出来）

---

## 整体流程（train_mnist.py 中的模型）

```
28×28 图片
  ↓ Conv2d(5×5, 10个kernel)
10张 24×24 特征图（检测到了边缘、曲线等）
  ↓ MaxPool2d(2)
10张 12×12 特征图（压缩了）
  ↓ 展平成 10×12×12 = 1440 个数字
  ↓ 全连接层 Linear(1440 → 10)
10个数字（分别代表 0-9 每个数字的概率）
```

一句话总结：**Conv2d 是"看局部特征的眼睛"，MaxPool2d 是"把看到的东西浓缩总结"**。

---

## 训练循环五步

```python
optimizer.zero_grad()           # 1. 清空上一步的梯度
output = model(data)            # 2. 前向传播（模型做出预测）
loss = criterion(output, target)# 3. 计算损失（误差有多大）
loss.backward()                 # 4. 反向传播（计算每个参数该怎么改）
optimizer.step()                # 5. 更新参数（真正迈出修正的一步）
```

---

## 涉及的知识点清单

| 类别 | 知识点 | 代码对应 |
|------|--------|---------|
| 深度学习 | 卷积层提取特征 | `nn.Conv2d(1, 10, kernel_size=5)` |
| 深度学习 | 池化层降维 | `nn.MaxPool2d(2)` |
| 深度学习 | 激活函数 | `torch.relu()` |
| 深度学习 | 全连接层分类 | `nn.Linear(10*12*12, 10)` |
| 深度学习 | 交叉熵损失 | `nn.CrossEntropyLoss()` |
| 深度学习 | SGD 优化器 | `optim.SGD(lr=0.01)` |
| PyTorch | GPU 数据搬运 | `.to(device)` |
| PyTorch | 数据加载 | `DataLoader(batch_size=64)` |
| PyTorch | 数据预处理 | `transforms.Normalize()` |
| GPU | CUDA 设备检测 | `torch.cuda.is_available()` |

---

## 核心概念详解（手算例子）

### 设定：最简单的神经网络

```
输入 x=2.0    权重 w=0.5    真实答案 y=4.0

预测: ŷ = x × w = 2.0 × 0.5 = 1.0
损失: loss = (ŷ - y)² = (1.0 - 4.0)² = 9.0

模型猜 1.0，答案是 4.0，错得很离谱（loss=9）
```

### 1. 反向传播 / 链式法则 — loss.backward()

**问题**：loss=9 太大了，w 应该往哪调？调多少？

链式法则 = 把复合函数的导数拆成一步步乘：

```
loss = (ŷ - y)²     其中 ŷ = x × w

∂loss/∂w = ∂loss/∂ŷ × ∂ŷ/∂w
              ↑           ↑
         loss对预测值   预测值对w
         多敏感         多敏感
```

手算：

```
∂loss/∂ŷ = 2×(ŷ - y) = 2×(1.0 - 4.0) = -6.0    ← 负数=ŷ应该变大
∂ŷ/∂w   = x = 2.0                                ← w变1，ŷ变2
∂loss/∂w = -6.0 × 2.0 = -12.0                    ← w增大→loss减小
```

**`loss.backward()` 算出来的就是这个**：`w.grad = -12.0`

多层时，就多乘几次：
```
∂loss/∂w1 = ∂loss/∂ŷ × ∂ŷ/∂中间 × ∂中间/∂w1
```
每层贡献一个乘数，一环扣一环 — 这就是"链式"的意思。

### 2. optimizer.step() — 参数怎么更新

公式就一行：**新w = 旧w - lr × 梯度**

```
w     = 0.5
grad  = -12.0
lr    = 0.01

新 w = 0.5 - 0.01 × (-12.0) = 0.5 + 0.12 = 0.62
                                      ↑
                              梯度是负的，所以w增大（对的！）

验证：
新预测 = 2.0 × 0.62 = 1.24
新loss = (1.24 - 4.0)² = 7.62
loss: 9.0 → 7.62  ✅ 下降了！
```

再来几百次，w 慢慢接近 2.0（因为 2×2=4 才是正确答案）。

### 3. 学习率为什么重要

```
梯度 = -12.0

lr=0.01:    新w = 0.5 + 0.12 = 0.62      ✅ 稳步接近正确值(2.0)
lr=10.0:    新w = 0.5 + 120  = 120.5      ❌ 飞了！loss爆炸
lr=0.00001: 新w = 0.5 + 0.00012 = 0.50012 😴 蜗牛速度

lr太大: 0.5 → 120 → -50000 → ... (爆炸发散)
lr合适: 0.5 → 0.62 → 0.73 → ... → 2.0 (收敛)
lr太小: 0.5 → 0.50012 → 0.50024 → ... (天黑了还没到)
```

**lr = 下山每步迈多远。太大摔死，太小天黑了还没到。**

### 4. .to(device) — CPU/GPU 内存隔离

```
你家（CPU内存）              公司（GPU显存）
┌──────────┐               ┌──────────┐
│  图片数据  │    马路(PCIe)   │  模型权重  │
│  在这里   │ ───────────── │  在这里    │
└──────────┘               └──────────┘
```

矩阵乘法需要数据和权重在**同一张桌子上**。

```python
model.to("cuda")      # 模型在GPU
data = data.to("cuda") # 图片也搬到GPU → ✅ 可以算了

# 如果不搬：
# RuntimeError: Input type (torch.FloatTensor) and weight type 
# (torch.cuda.FloatTensor) should be the same
# 意思："输入在CPU，权重在GPU，够不着！"
```

---

## 训练的原理

训练 = **试错 + 修正**，反复用正确答案告诉模型"你错了，往这边调"。

### 核心循环：猜 → 算错多少 → 往对的方向调

```
第1次：模型瞎猜 → "这是7" → 答案是3 → 错得离谱 → 大幅调参数
第2次：模型再猜 → "这是3" → 答案是3 → 对了 → 几乎不调
...重复几万次...
最终：模型学会了
```

### 梯度下降（为什么能学会）

想象你闭着眼在山上，想走到最低点（loss=0）：

```
        你在这→ X
       ╱        ╲
      ╱    坡度   ╲
     ╱   (梯度)    ╲
    ╱                ╲
   ╱        最低点    ╲
──────────── ★ ────────────
```

- `loss.backward()` = 摸地面感受哪边是下坡（计算梯度）
- `optimizer.step()` = 往下坡方向迈一小步（lr=0.01 控制步子大小）
- 重复几千次 → 到达谷底（loss 最小 → 预测最准）

### 模型的"参数"是什么

Conv2d 的 5×5 kernel 的 25 个数字 + bias + 全连接层 1440×10 个数字。一开始随机，训练完变成能识别手写数字的特定数值。

---

## GPU 在做什么运算

**核心运算 = 矩阵乘法。** 主要在 forward 和 backward 里。

### Forward：GPU 算什么

**Conv2d — 卷积 = 大量乘加（展开成矩阵乘）**
```
kernel (5×5=25个数) × 图片上每个位置的25个像素 → 求和 → 1个输出值

一张图: 24×24 = 576 个位置，每个位置 25 次乘+加
10个kernel: 576 × 10 = 5760 次点乘
一个batch 64张图: 5760 × 64 = 368,640 次点乘
```

**Linear — 全连接层 = 纯矩阵乘法**
```
输入 [64, 1440] × 权重 [1440, 10] = 输出 [64, 10]
= 64 × 1440 × 10 = 921,600 次乘法
```

### Backward：也是矩阵乘（反向）

```
Forward:  output = input × weight
Backward: grad_input  = grad_output × weight.T    (矩阵乘)
          grad_weight = input.T × grad_output      (矩阵乘)
```

### 为什么 GPU 擅长这个

```
CPU: 几个核，串行算              GPU: 几千个核，同时算
A10G: 9216 个 CUDA 核，能同时做几千个乘加
```

矩阵乘法天然可并行（每个输出元素独立计算），所以 GPU 比 CPU 快 10-100×。

### 总结：深度学习里 GPU 做的事

```
Conv2d   → 卷积展开成矩阵乘
Linear   → 直接是矩阵乘
ReLU     → 简单比较(x>0?)，几乎不费算力
backward → 也是矩阵乘（反向）
```

---

## 与 Mini Serving Engine 项目的关系

这个脚本是**训练侧**的 hello world，验证 A10G 环境可用。

Mini Serving Engine 项目关注的是**推理侧**——拿训练好的 LLM 高效地逐 token 生成，核心挑战：
1. KV Cache 内存管理
2. 多请求 batching
3. Continuous batching 动态调度
